// Placeholder for helpers that map domain data to Recharts series.

export interface ChartPoint {
  label: string;
  value: number;
}

export const buildCareerPerformanceSeries = (): ChartPoint[] => {
  // TODO: derive from real data; this is just a stub.
  return [];
};
